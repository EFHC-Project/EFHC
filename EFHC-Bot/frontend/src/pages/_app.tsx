/**
======================================================================
== EFHC Bot WebApp — корневой слой Next.js                            ==
======================================================================
Назначение: подключает глобальные стили и безопасно оборачивает страницы, не затрагивая деньги или энергию напрямую.
Инварианты: клиент ничего не пересчитывает — только отображает данные бэкенда EFHC; запреты P2P и EFHC→kWh соблюдаются на сервере.
ИИ-защита: минимальный рендер без побочных эффектов, деградирует к статическому UI при отсутствии окружения; ошибки стилей не влияют на данные.
Запреты: не содержит бизнес-логики, не изменяет балансы, не инициирует денежные запросы.
======================================================================
*/
import type { AppProps } from "next/app";

import "../styles/globals.css";

function App({ Component, pageProps }: AppProps) {
  // Легковесная обёртка: без провайдеров, чтобы не ломать SSR, UI только показывает данные.
  return <Component {...pageProps} />;
}

export default App;

// ======================================================================
// Пояснения «для чайника»:
//   • Этот файл лишь подключает глобальные стили и рендерит страницы.
//   • Денежные операции и кWh здесь не происходят — они на бэкенде EFHC.
//   • При ошибках стилей приложение остаётся доступным (UI без интерактива).
//   • Любые финансовые запросы должны идти через API слоёк с Idempotency-Key.
// ======================================================================
