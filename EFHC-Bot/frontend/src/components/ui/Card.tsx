/**
======================================================================
== EFHC Bot WebApp UI — карточка                                      ==
======================================================================
Назначение: контейнер для отображения контента без логики денег.
Инварианты: только визуальный блок; не инициирует сетевые или денежные операции.
ИИ-защита: без сторонних зависимостей, безопасен при пустых children.
Запреты: не изменяет состояние приложения.
======================================================================
*/
import type { ReactNode } from "react";

type Props = {
  children: ReactNode;
};

function Card({ children }: Props) {
  return <div>{children}</div>;
}

export default Card;

// ======================================================================
// Пояснения «для чайника»:
//   • Карточка — просто оболочка для UI-элементов.
//   • Балансы и начисления здесь не трогаются.
//   • Можно использовать для списков панелей, задач, витрин.
// ======================================================================
